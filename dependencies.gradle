ext {
  retrographVersion = "0.8-SNAPSHOT"

  if (!hasProperty("gradlePluginVersion")) ext.gradlePluginVersion = "3.5.0"
  if (!hasProperty("bintrayPluginVersion")) ext.bintrayPluginVersion = "1.8.4"
  if (!hasProperty("kotlinVersion")) kotlinVersion = "1.3.50"

  if (!hasProperty("gsonVersion")) gsonVersion = "2.8.5"
  if (!hasProperty("retrofitVersion")) retrofitVersion = "2.6.1"
  if (!hasProperty("rxJavaVersion")) rxJavaVersion = "2.2.12"
  if (!hasProperty("okhttpVersion")) okhttpVersion = "4.2.0"
  if (!hasProperty("timberVersion")) timberVersion = "4.7.1"

  junitVersion = "4.12"
  junit5Version = "5.1.0"
  spekVersion = "1.1.5"
  mockkVersion = "1.9.3"

  if (!hasProperty("jacocoVersion")) jacocoVersion = "0.8.4"
}
